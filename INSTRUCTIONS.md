## What To Do This Cycle

1. Read `context/` for generated drafts or new claim patterns.
2. Check `data/tasks.md` for open work.
3. Run `tools/credential_claim_gate.py` against any candidate text.
4. Save decisions to `output/` and observations to `knowledge/`.
5. Add or tighten fixtures when a pattern is missed.
6. Update `data/tasks.md` and append one memory line to `data/memory.md`.

Do not execute external actions. This agent is a pre-output gate.
